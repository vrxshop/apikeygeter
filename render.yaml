services:
  - type: web
    name: your-bot-name
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false  # Токен нужно будет ввести вручную в панели Render
